---
# Molecule converge — exercises the ai_runner role. Secrets are left empty so
# the worker installs and starts but stays idle (no live Vikunja needed).

- name: Converge
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Include ai_runner role
      ansible.builtin.include_role:
        name: ai_runner
