---
# recreate: always so a compose-file change lands even when the running
# container is healthy (same reason as agentgateway_docker).
- name: Restart docling-serve
  community.docker.docker_compose_v2:
    project_src: "{{ docling_serve_data_dir }}"
    state: present
    recreate: always
