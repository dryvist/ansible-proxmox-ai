---
# --- OpenCode Go flat-rate subscription tier ($10/mo) ------------------------
# Sunk-cost subscription: MUST be exhausted before metered API usage (OpenRouter).
# Order priority 20 (within the 20-39 subscription tier), placed after truly
# local within-network models (01-19) and before metered APIs (70-99).

llm_router_opencode_base_url: "https://opencode.ai/zen/go/v1"
llm_router_opencode_credential_env: "OPENCODE_API_KEY"
llm_router_opencode_default_order: 20
llm_router_opencode_rpm: 60
llm_router_opencode_tpm: 500000
llm_router_opencode_max_parallel_requests: 4
