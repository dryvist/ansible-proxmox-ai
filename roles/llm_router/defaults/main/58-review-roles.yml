---
# EXTERNAL TERMINAL RUNGS, cost order, credential-gated: the subscription
# rung, then every paid cloud rung in registry order — ending on the provider
# API-key rung (codex-api). Every chat ladder below ends with this list, and
# router_settings.default_fallbacks carries it for any name that has no
# ladder of its own, so no request can dead-end on a busy local rung.
#
# The FREE cloud rungs are derived here but sit in NO ladder: measured on the
# log platform, a free endpoint refuses the ladder's requests on the
# provider's parameter and data-policy filters, gates agentic callers, and
# the free key hits its daily cap — a rung that answers 404/403 every time
# only spends the walk. Keep them out until a rung is measured answering.
llm_router_external_free_rungs: >-
  {{ (llm_router_hermes_cloud_free_groups + llm_router_openrouter_free_groups) | unique | list }}
llm_router_external_paid_rungs: >-
  {{ (llm_router_hermes_cloud_paid_groups + llm_router_hermes_cloud_api_key_groups) | unique | list }}
# The subscription rung (36-hermes-cloud-segments.yml) sits between free and
# paid: a metered rung is reached only after the subscription answers 429.
llm_router_external_subscription_rungs: "{{ llm_router_hermes_cloud_subscription_groups | unique | list }}"
llm_router_external_terminal_rungs: >-
  {{ llm_router_external_subscription_rungs + llm_router_external_paid_rungs }}
llm_router_default_fallbacks: "{{ llm_router_external_terminal_rungs }}"

# Names with no same-modality external rung to escape to: the two DB-seeded
# "fail FAST" roles (55-roles.yml) plus the git-registry group behind the
# embedding one (a caller can name either). An EXACT fallbacks entry
# short-circuits default_fallbacks in litellm's get_fallback_model_group, so
# a busy/failed call under one of these names never walks the five-rung chat
# ladder above — cross-modality errors an embedding/OCR failure has no
# business paying for, at chat-ladder latency and, for codex-api, spend.
#
# PLUS `free-zdrless`, the ZDR-false half of the standalone `free` /
# `free-zdrless` pair (model-list-free.yaml.j2): it fails fast the same
# way embed/ocr do. `free` is NOT in this set — it renders its own
# explicit empty entry directly in config.yaml.j2 (never reachable from
# `free-zdrless`, by design: the hindsight key that calls `free` must
# never reach a zero_data_retention: false rung).
llm_router_cross_modality_fail_fast_names: >-
  {{ ['embed', 'ocr', 'free-zdrless'] + [llm_router_embedding_model] }}

# `review-local` and `local`, same short-circuit mechanism as the
# cross-modality names above but a different reason: neither is another
# modality, both are callers that explicitly want NO external rung ever,
# local backend up or down. A separate list rather than folding it into the
# cross-modality one, so each name's rendered comment states its own reason.
llm_router_local_only_fail_fast_names:
  - review-local
  - local

# ONE variable for the whole shared external continuation (2026-09-20
# operator decision, 35-hermes-cloud.yml): the Z.ai pair, the ZDR paid
# OpenRouter group, then the subscription/API-key terminal every chain
# already ends in — exactly the four things the operator named, NOT
# llm_router_external_terminal_rungs' full paid segment (that also carries
# hermes-cloud-alibaba/hermes-cloud-gemini, a separate, older
# provider-diverse-value feature with no relation to this continuation).
# Filtering llm_router_hermes_cloud_paid_groups down to the one matching
# name, rather than typing 'hermes-cloud-openrouter' as a fresh literal,
# keeps this credential-gated the same way every other member already is —
# renders empty the moment OPENROUTER_HERMES_KEY is unseeded.
#
# Reachable ONLY through the role rows (review-oss/review-private,
# 55-/58-roles.yml) and the ZDR-paid-OpenRouter isolation above — NOT used
# by llm_router_hermes_own_fallback_by_group below, which derives every
# group's own tail directly from the chain itself instead.
llm_router_shared_external_continuation: >-
  {{ (llm_router_zai_groups
      + (llm_router_hermes_cloud_paid_groups | select('equalto', 'hermes-cloud-openrouter') | list)
      + llm_router_external_subscription_rungs
      + llm_router_hermes_cloud_api_key_groups) | unique | list }}

# THE FIX for litellm 1.98's nested-fallback-follows-the-failed-rung's-own-
# chain behavior (router.py::async_function_with_fallbacks): once a walk is
# UNDER a group's own name (e.g. hermes-default's chain advanced to it, or
# it was reached as review-oss's second attempt), a further failure on THAT
# group consults the group's OWN key in the fallbacks map, not the chain
# that reached it. Before this fix every non-local group's own key pointed
# at [llm_router_primary_model] (config.yaml.j2's per-group loop) — wrong
# for ANY group in the chain, not only the shared external continuation:
# the interactive caller's first hop past the primary is often
# hermes-cloud-alibaba, and an alibaba 429 walking its old [primary] entry
# hit the identical cap, one rung earlier.
#
# GENERALIZED (2026-09-21): own(X), for every non-local group X in
# llm_router_hermes_default_fallback_chain, is that SAME chain's own tail
# after X, local rungs filtered out (they are never reachable a second time
# this way — a caller already past X has no reason to re-enter one). The
# shared external continuation above is nothing but the chain's own last
# members, so it needs no separate expression here; the four-rung mapping
# this fixed originally is simply what this produces for X in
# {zai-glm-4.5-flash, zai-glm-5.3-flash, hermes-cloud-openrouter, codex-api}.
#
# ZDR: a group that is ALSO one of the caller-typed entry points
# (llm_router_zdr_entry_point_names, 37-fallback-entry-points.yml) drops the
# free-zdrless segment from its own tail too, same rule config.yaml.j2
# already applies to every entry point's ladder — a rung promising no data
# leaves the network must never fall through to one making no such promise.
# One dict, built by index rather than hand-listed pairs, keyed by every
# group the chain actually contains — a chain member added or reordered in
# 35-hermes-cloud.yml gets its own correct tail with no second place to
# update, and config.yaml.j2's per-group loop (its only consumer, plus the
# context-window escape below) needs no `else [primary]` branch at all.
llm_router_hermes_own_fallback_by_group: >-
  {%- set ns = namespace(map={}) -%}
  {%- set _chain = llm_router_hermes_default_fallback_chain -%}
  {%- for _group in _chain if not _group.startswith('hermes-local') -%}
  {%- set _idx = _chain.index(_group) -%}
  {%- set _tail = _chain[_idx + 1 :] | reject('match', '^hermes-local') | list -%}
  {%- if _group in llm_router_zdr_entry_point_names -%}
  {%- set _tail = _tail | reject('in', llm_router_openrouter_free_groups) | list -%}
  {%- endif -%}
  {%- set _discard = ns.map.update({_group: _tail}) -%}
  {%- endfor -%}
  {{ ns.map }}

# Keys of the above whose own tail is non-empty — the ones that need a
# context_window_fallbacks escape too (37-fallback-entry-points.yml) and the
# ones a provider-scoped render must still key (test_hermes_provider_keys.yml).
llm_router_chain_groups_with_own_tail: >-
  {{ llm_router_hermes_own_fallback_by_group | dict2items
     | selectattr('value') | map(attribute='key') | list }}

# REVIEW ROLES — local first, always. `review-oss` overflows to the Z.ai
# pair (33-zai.yml) — still no free rung (see above) — and, if the pair
# itself fails (both members busy, or the paid member's plan/quota is
# exhausted), on to the same paid terminal every other public chain ends
# in. That escape is a single conditional, not a hand-appended list: it
# fires only when the chain's own last rung is not already one of
# llm_router_external_terminal_rungs, so a chain that already ends in a
# subscription/paid rung (subagent/fast/judge/cheap above) is untouched,
# and review-oss stops escaping the moment its own terminal changes to
# something already covered.
# `review-private` may leave the estate ONLY for the Codex rungs: the
# subscription first, the API key last — no OpenRouter, no free tier, no
# Z.ai, ever. Until a Codex rung is credentialed it renders local-only and
# fails fast; tests/llm_router/test_review_roles.yml states both contracts.
_llm_router_review_oss_chain: >-
  {%- set _chain = [llm_router_primary_model] + llm_router_zai_groups -%}
  {{ _chain + (llm_router_external_paid_rungs
               if (_chain | length > 1
                   and _chain[-1] not in llm_router_external_terminal_rungs)
               else []) }}
_llm_router_review_private_chain: >-
  {{ [llm_router_primary_model] + llm_router_external_subscription_rungs
     + llm_router_hermes_cloud_api_key_groups }}
