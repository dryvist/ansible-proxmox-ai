---
galaxy_info:
  author: JacobPEvans
  description: Per-guest secrets for the herdr agent-multiplexer runtime; the guest itself is converged by nixos_deploy
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - herdr
    - agent
    - nixos
dependencies: []
