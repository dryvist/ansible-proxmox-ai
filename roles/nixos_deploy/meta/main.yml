---
galaxy_info:
  author: JacobPEvans
  description: Converge a NixOS guest with nixos-rebuild --target-host, run on the controller
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - nixos
    - nix
    - deploy
dependencies: []
