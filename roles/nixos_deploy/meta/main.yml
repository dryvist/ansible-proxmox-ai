---
galaxy_info:
  role_name: nixos_deploy
  description: >-
    Converge a NixOS guest by running nixos-rebuild --target-host from the
    Ansible controller. Generic: takes a flake ref and a host attribute.
  license: MIT
  min_ansible_version: "2.15"
dependencies: []
