---
galaxy_info:
  author: JacobPEvans
  description: >-
    Shared Redis instance backing the LiteLLM router pool's cross-instance spend
    accounting. Not a general-purpose cache — see the role README for why it is
    a separate guest rather than colocated.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - ai
    - redis
    - llm
dependencies: []
