---
# hermes-ui Docker handlers

- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: Restart hermes-ui
  community.docker.docker_compose_v2:
    project_src: "{{ hermes_ui_docker_data_dir }}"
    state: present
    recreate: always
